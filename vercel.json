{
  "version": 2,
  "builds": [
    { "src": "apps/swapture/package.json", "use": "@vercel/static-build", "config": { "distDir": "apps/swapture/dist" } }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/apps/swapture/dist/$1" }
  ]
}
