{
  "name": "swapture",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm run dev --workspace=apps/swapture",
    "build": "npm run build --workspace=apps/swapture",
    "generate-tool": "ts-node scripts/generate-tool.ts"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0"
  }
}
